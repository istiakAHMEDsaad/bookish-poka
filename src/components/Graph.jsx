import { BarChart, Bar, Cell, XAxis, YAxis, CartesianGrid } from 'recharts';

const Graph = ({ para }) => {
  const { bookName, totalPages } = para;

  
  return (
    <>
      
    </>
  );
};

export default Graph;
